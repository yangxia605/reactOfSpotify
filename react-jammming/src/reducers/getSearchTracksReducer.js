/** * Created by mianmian on 2018/3/27. */import {  ADD_TRACK,  GET_SEARCH_TRACKS,  REMOVE_TRACK,} from '../constants/ActionTypes'import _ from 'lodash'const initialState = {  searchResults: [],  playlistTracks: []};const getSearchTracksReducer = (state = initialState, action={type: '', data: ''}) => {  const {type, data } = action;  switch (type){    case GET_SEARCH_TRACKS:      return {        ...state,        searchResults: data      };    case ADD_TRACK:      const tracks =[...state.searchResults];      tracks.splice(tracks[data],1);      return {        ...state,        searchResults:tracks,        playlistTracks: _.concat(state.playlistTracks,data)      };    case REMOVE_TRACK:      const playlistTracks =[...state.playlistTracks];      playlistTracks.splice(playlistTracks[data],1);      return {        ...state,        searchResults: _.concat(state.searchResults,data),        playlistTracks:playlistTracks      }    default:      return state  }}export default getSearchTracksReducer