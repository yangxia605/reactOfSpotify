/** * Created by mianmian on 2018/3/27. */import Spotify from '../util/Spotify'import * as types from '../constants/ActionTypes';//adds a song to the playlistexport const addTrack = (track) => ({  type: types.ADD_TRACK,  data:track});// removes a song from a user's custom playlistexport const removeTrack = (track) => ({  type: types.REMOVE_TRACK,  data:track});export const updatePlaylistName = (playlistName,) => ({  type: types.UPDATE_PLAYLIST_NAME,  playlistName});export const userSavePlaylist = (playlistName, trackUris) => (dispatch) =>{  dispatch({type: types.USER_SAVE_PLAYLIST});  Spotify.savePlaylist(playlistName,trackUris).then(response => {    if(response){      dispatch({        type: types.RESET_PLAYLIST_NAME      })    }  })};export const getSearchTracks = (data) => ({  type: types.GET_SEARCH_TRACKS,  data: data});export const search = ( term ) => (dispatch) => {  Spotify.search(term).then(tracks => {    if(tracks){      dispatch(getSearchTracks(tracks))    }  })};export const getPlaylist = () => (dispatch) => {  dispatch({    type: types.GET_PLAYLIST  })}