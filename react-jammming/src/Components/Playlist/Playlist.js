/** * Created by mianmian on 2018/3/15. */import React, {Component} from 'react';import './Playlist.css';import TrackList from '../TrackList/TrackList'class Playlist extends Component {  constructor(props){    super(props);  }  handleNameChange = (e) =>{    this.props.onNameChange(e.target.value);    this.setState({      value: e.target.value    })  };  render(){    const { playlistTracks,onRemove, onSave, playlistName} = this.props;    return(      <div className="Playlist">        <input value={playlistName ? playlistName : 'New Playlist'} onChange={this.handleNameChange}/>        <TrackList          isRemoval={true}          tracks={playlistTracks}          onRemove={onRemove} />        <a          className="Playlist-save"          onClick={onSave}        >          SAVE TO SPOTIFY        </a>      </div>    )  }}export default Playlist;