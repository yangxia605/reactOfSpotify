/** * Created by mianmian on 2018/3/15. */import React from 'react';import './Playlist.css';import { connect  }from 'react-redux'import { bindActionCreators } from 'redux'import TrackList from '../../Components/TrackList/TrackList'import { removeTrack, userSavePlaylist } from '../../actions'class Playlist extends React.Component {  render(){    return(      <div className="Playlist">        <input  />        <TrackList          isRemoval={true}          tracks={this.props.playlistTracks}          onRemove={this.props.removeTrack} />        <a          className="Playlist-save"          onClick={this.props.userSavePlaylist}        >          SAVE TO SPOTIFY        </a>      </div>    )  }}const mapStateToProps = (state) => ({  playlistTracks: state.getSearchTracksReducer.playlistTracks,});const mapDispatchToProps = (dispatch) => ({  removeTrack: bindActionCreators(removeTrack, dispatch),  userSavePlaylist: bindActionCreators(userSavePlaylist, dispatch)})const Playlists = connect(  mapStateToProps,  mapDispatchToProps)(Playlist)export default Playlists;