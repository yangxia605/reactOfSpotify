/** * Created by mianmian on 2018/3/15. */import React from 'react';import './Playlist.css';import { connect  }from 'react-redux'import TrackList from '../../Components/TrackList/TrackList'import { removeTrack } from '../../actions'const Playlist = ({playlistName,onRemove, onSave,playlistTracks,}) => {  return(    <div className="Playlist">      <input  />      {/*<TrackList*/}        {/*isRemoval={true}*/}        {/*tracks={playlistTracks}*/}        {/*onRemove={onRemove} />*/}      <a        className="Playlist-save"        onClick={onSave}      >        SAVE TO SPOTIFY      </a>    </div>  )}const mapStateToProps = (state) => ({  playlistTracks: state.playlistTracks,});const mapDispatchToProps = (dispatch,track) => ({  onRemove: () => {    dispatch(removeTrack(track))  }})const Playlists = connect(  mapStateToProps,  mapDispatchToProps)(Playlist)export default Playlists;