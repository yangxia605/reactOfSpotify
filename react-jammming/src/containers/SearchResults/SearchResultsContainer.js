/** * Created by mianmian on 2018/3/15. */import React, {Component} from 'react'import { connect  }from 'react-redux'import { bindActionCreators } from 'redux'import './SearchResults.css';import TrackList from '../../Components/TrackList/TrackList'import { addTrack } from '../../actions/index'class SearchResults extends Component {  constructor(props){    super(props);  }  render(){    const { searchResults, addTrack }= this.props;    return(      <div className="SearchResults">        <span>Results</span>        <TrackList          isRemoval={false}          onAdd={addTrack}          tracks={searchResults} />      </div>    )  }}const mapStateToProps = (state) => {  return {    searchResults: state.getSearchTracksReducer.searchResults  }};const mapDispatchToProps = (dispatch) => ({  addTrack: bindActionCreators(addTrack, dispatch)});const SearchResultsContainer = connect(  mapStateToProps,  mapDispatchToProps)(SearchResults);export default SearchResultsContainer;