/** * Created by mianmian on 2018/3/15. */import React, {Component} from 'react'import { connect  }from 'react-redux'import './SearchResults.css';import TrackList from '../../Components/TrackList/TrackList'import { addTrack } from '../../actions/index'import getSearchTracksReducer from '../../reducers/getSearchTracksReducer'class SearchResults extends Component {  constructor(props){    super(props);  }  render(){    const { searchResults, addTrack }= this.props;    return(      <div className="SearchResults">        <span>Results</span>        <TrackList          isRemoval={false}          onAdd={addTrack}          tracks={searchResults.searchResults} />      </div>    )  }}const mapStateToProps = (state) => {  return {    searchResults: getSearchTracksReducer(state.searchResults)  }};const mapDispatchToProps = (dispatch) => {  return {    addTrack: (track) => {      dispatch(addTrack(track))    }  }};const SearchResultsContainer = connect(  mapStateToProps,  mapDispatchToProps)(SearchResults);export default SearchResultsContainer;